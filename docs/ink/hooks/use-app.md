---
topic: ink/hooks/use-app
title: useApp Hook
description: "`useApp` is a React hook that exposes a method to manually exit
  the app (unmount)."
version: 5.x
sourceUrl: https://github.com/vadimdemedes/ink
tags:
  - ink
  - hooks
---

### useApp()

`useApp` is a React hook that exposes a method to manually exit the app (unmount).